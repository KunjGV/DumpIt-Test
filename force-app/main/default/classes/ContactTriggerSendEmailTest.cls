@isTest
public class ContactTriggerSendEmailTest {
	@isTest
    public static void testLogic(){
        
        List<Contact> newCons = TestDataFactory.createContacts(3);
        insert newCons;
        
        
    }
}